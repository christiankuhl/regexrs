# regexrs -- The regex library no one asked for

This is an educational project, aimed at parsing regexes in Python syntax,
converting them into finite state machines and executing them for simple
pattern matching.